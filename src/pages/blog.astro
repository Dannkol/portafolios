---
import BlogListing from "../components/BlogListing.astro";

const posts = Object.values(import.meta.glob("../posts/*.mdx", { eager: true }))
  .filter((post: any) => post.frontmatter.lang !== "es") as any[];
---

<BlogListing lang="en" posts={posts} />
